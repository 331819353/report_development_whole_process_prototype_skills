interface:
  display_name: "报表原型模板管理"
  short_description: "选择、复制、配置和校验可运行 Vue/Vite 报表模板，仅允许交互和组件内容区模板例外扩展。"
  default_prompt: "Use $report-prototype-template-management to select, copy, configure, validate, and preview bundled Vue 3 + TypeScript + Vite + Element Plus + ECharts + axios report templates with implementationMode copyTemplateProject by default, while preserving PRD RTP/PATH reading-path traces and applicable ESG/SEV/ACT/TRUST/MEET executive satisfaction gate traces in pageLayoutConfig/blockLayoutTemplateMap, treating PRD/source HTML as evidence unless the latest user explicitly requests HTML/static output, rejecting blank new Vue3 projects unless documented self-development exceptions require them, enforcing template-only shell/layout/block/supporting-area rules, allowing only interaction behavior or component content area template exceptions, requiring docs/prototype-data-summary.md for backend-facing data handoff, running ledger:init after copy and ledger:check before handoff, and using __change_logs__ sidecar ledgers for changed source files."

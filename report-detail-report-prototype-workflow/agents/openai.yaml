interface:
  display_name: "明细报表原型 Workflow"
  short_description: "基于 PRD 执行矩阵编排明细报表原型，覆盖行粒度、筛选、表格、导出、追溯和运行QA。"
  default_prompt: "Use $report-detail-report-prototype-workflow to design or build a detail report prototype from the prerequisite $report-prd-document-generation PRD, covering row grain, filters, table fields, sorting, pagination, TRUST source/freshness/row identity, export/audit scope, permissions, traceability, row actions, states, template-only layout/block configuration, copyTemplateProject-first bundled Vue 3 + TypeScript + Vite + Element Plus + ECharts + axios runtime output, PRD/source HTML treated only as evidence unless the latest user explicitly requests HTML/static output, new Vue3 project only for documented self-development exceptions, and runtime QA."

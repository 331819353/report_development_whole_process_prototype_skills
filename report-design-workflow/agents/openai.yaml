interface:
  display_name: "报表原型 Workflow"
  short_description: "基于 PRD 执行矩阵编排报表/仪表盘/大屏原型，再进入类型、组件映射、模板、实现、运行QA和URL交付。"
  default_prompt: "Use $report-design-workflow to orchestrate generic report prototype work from the prerequisite $report-prd-document-generation PRD and PRD-to-workflow execution matrix, then proceed through PRD RTP/PATH report-type implementation path consumption, ESG/SEV/ACT/TRUST/MEET executive satisfaction gate consumption when management-facing, prototype thinking, report type, dynamic conclusion-rule mapping, component mapping, template-only layout and block configuration, copyTemplateProject-first bundled Vue 3 + TypeScript + Vite + Element Plus + ECharts + axios implementation, ledger:init after template copy and ledger:check before handoff, PRD/source HTML treated only as evidence unless the latest user explicitly requests HTML/static output, new Vue3 project only for documented self-development exceptions, docs/prototype-data-summary.md backend-facing data handoff, runtime QA, and URL handoff."

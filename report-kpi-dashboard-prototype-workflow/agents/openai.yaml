interface:
  display_name: "指标看板原型 Workflow"
  short_description: "基于 PRD 执行矩阵编排指标看板原型，覆盖KPI、趋势排名、异常预警、下钻和运行QA。"
  default_prompt: "Use $report-kpi-dashboard-prototype-workflow to design or build a KPI dashboard prototype from the prerequisite $report-prd-document-generation PRD, covering current-state decisions, ESG 3-second answer/30-second diagnosis/3-minute action, SEV severity, ACT closure, TRUST freshness/source, KPI dictionary, targets, trends, rankings, anomaly rules, drilldowns, filters, permissions, template-only layout/block configuration, copyTemplateProject-first bundled Vue 3 + TypeScript + Vite + Element Plus + ECharts + axios runtime output, PRD/source HTML treated only as evidence unless the latest user explicitly requests HTML/static output, new Vue3 project only for documented self-development exceptions, and runtime QA."
